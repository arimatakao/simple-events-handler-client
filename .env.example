# Example environment variables for the React app
# Set the backend API base URL (include /api if your server exposes it there)
REACT_APP_API_BASE_URL=http://localhost:8080/api
# Timezone for from/to fields. Supported: LOCAL (default), UTC, or offset like +03:00
REACT_APP_TIMEZONE=LOCAL
